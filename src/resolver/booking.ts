// booking: [Booking!]!
// bookEvent(eventId: ID!): Booking!
// cancelBooking(bookingId: ID!): Event!

export default {
    booking: () => {

    },
    bookEvent: (eventId: string) => {

    },
    cancelBooking: () => {

    }
}